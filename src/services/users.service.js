import { usersRepository } from "../repositories/index.js";

export default class UsersService{
    constructor(){
        this.usersRepository = usersRepository;
    }
    // TO DO : Crear el usersService
}